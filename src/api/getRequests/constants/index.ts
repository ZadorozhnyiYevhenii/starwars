export enum Errors {
  FILMS = "",
  HERO = "",
  HEROS = "",
  SPACESHIP = "Could not fetch the starships",
}

export enum Endpoints {
  FILMS = "films",
  PEOPLE = "people",
  STARSHIPS = "starships",
}
